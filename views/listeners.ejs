<!doctype html>
<html>
  <head>
    <title><%=shopName%> Shop</title>
    <link rel="stylesheet"  type="text/css" href="main.css" />
  </head>
  <body>
    <h1>About <%=shopName%></h1>
    <h2>These are the logins</h2>
    <ul>
        <% for(let i = 0; i < availableLogins.length; i++) { %>
            <li><%= availableLogins[i].first%> , £<%= availableLogins[i].last%> , £<%= availableLogins[i].email%> , £<%= availableLogins[i].username%></li>
            <% } %>
        </ul>
  </body>
</html>